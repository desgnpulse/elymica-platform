name = "elymica-student-portal"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# Cloudflare Pages configuration
pages_build_output_dir = "apps/student-portal/.open-next"

[vars]
# Surface to the worker so Next.js knows the preferred runtime
NEXT_RUNTIME = "nodejs"

[env.production]
name = "elymica-student-portal"

[env.preview]
name = "elymica-student-portal-preview"
